spring.application.name=rolesandpermissionsv2
spring.profiles.active=dev

# Supabase PostgreSQL Database Configuration
spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?user=postgres.rswovqretgzwdzejjgqr&password=rolesandpermissionsv2}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings (Spring Boot default DataSource - used as fallback)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# PostgreSQL DSLFactory Configuration (for multi-tenant DataSource management)
postgres.default.min.connections=5
postgres.default.max.connections=20
postgres.default.connection.timeout=30000
postgres.default.idle.timeout=600000
postgres.default.max.lifetime=1800000

# HikariCP Executor Service Configuration
postgres.hikari.executor.pool.size.production=100
postgres.hikari.executor.pool.size.non-production=20

# JOOQ Configuration
spring.jooq.sql-dialect=POSTGRES